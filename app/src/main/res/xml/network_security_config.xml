<?xml version="1.0" encoding="utf-8"?>
<!--
  Android blocks plain-HTTP traffic by default (since API 28). The only
  exception we make is the emulator's alias for the host machine, so a debug
  build can talk to the API on localhost:3000. Everything else stays HTTPS-only,
  which also protects release builds if this file is ever loaded there.
-->
<network-security-config>
    <base-config cleartextTrafficPermitted="false" />
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
    </domain-config>
</network-security-config>
