*.iml
.gradle
/local.properties
.idea/
.DS_Store
build/
/captures
.externalNativeBuild
.cxx
local.properties

# Added for ABC Mailbox
/.kotlin
*.hprof
.env
/tools/*.token

# Node tooling
/tools/node_modules
/tools/dev-group-key.json

# Release signing: never commit these
/keystore.properties
*.jks
*.keystore

# Firebase project values: they belong to whoever runs the network
/firebase.properties
google-services.json

# JVM crash reports (a Gradle daemon that died leaves these in the project folder)
hs_err_pid*.log
replay_pid*.log
